<h1>fcd/read</h1>
<p>Find me in app/views/fcd/read.html.erb</p>

<%= link_to 'Refresh', 'read' %>
  <% 4.times do %>
  <!-- # ///////////////////////////////Move this method to user drawing -->
    <% rbhand = []
      back = "Bluedrag.jpeg"
      5.times do
      rbhand << "#{draw}"
      end %>
  <!-- #////////////////////////////////////////////// -->
<div>
  <table>
    <% rbhand.each do |hcard| %>
    <td>
      <div class="flip-container" onclick="this.classList.toggle('flip');">
        <div class="flipper">

          <div class="back">
            <%= image_tag(back, width: 112.5, height: 157.5) %>
            <% @flip = 0 %>
          </div>

          <div class="front">
            <%= image_tag(hcard + ".png", width: 112.5, height: 157.5) %>
            <% @flip = 1 %>
          </div>

        </div>
      </div>
    </td>
    <% end %>
  </table>
</div>
<%= rbhand %>
<%= readhand(rbhand) %>
<% end %>
